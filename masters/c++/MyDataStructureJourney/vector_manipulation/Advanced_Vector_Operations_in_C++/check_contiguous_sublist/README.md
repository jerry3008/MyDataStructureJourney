# Check Contiguous Sublist

This C++ program checks if one vector (`listB`) is a contiguous sublist of another vector (`listA`) using only basic loops and comparisons.

## Example

```cpp
isContiguousSublist({1, 2, 3, 4, 5}, {3, 4}) // returns true
