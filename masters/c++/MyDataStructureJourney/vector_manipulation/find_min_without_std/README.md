# Find Minimum Without std::min_element

This utility function implements a minimum finder without using any built-in `std::min_element`.

## How to run
Compile and run:

```bash
g++ main.cpp -o find_min && ./find_min
